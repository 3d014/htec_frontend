
import { Box } from '@mui/material';

// Test component
const Test = () => {

    return (
        <Box sx={{ display: 'flex', flexDirection: 'column' }}>
           test
        </Box>
    );
};

export default Test;
